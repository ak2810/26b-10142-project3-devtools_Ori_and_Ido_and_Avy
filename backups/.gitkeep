This folder holds the backup artifacts required by Part 7.3 of the assignment:

  my-drupal.backup.sql.gz   - the MySQL database dump
  drupal-sites.tar.gz       - the Drupal site files / design (sites volume)

Both are generated by running ./backup.sh after the site has been configured
and filled with content. Commit them to the repository once created.
